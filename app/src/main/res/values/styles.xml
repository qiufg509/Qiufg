<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="WelcomeTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/welcome_bg</item>
    </style>

    <style name="TabLayoutTextStyle" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textColor">@color/category_tab_text_selector</item>
    </style>

    <style name="Dialog_Divider">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">@dimen/dialog_divider</item>
        <item name="android:background">@color/dialog_divider</item>
    </style>

    <style name="Mine_Divider_Long">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">0.5dp</item>
        <item name="android:background">@color/button_bg_press</item>
    </style>

    <style name="Mine_Divider_Short" parent="Mine_Divider_Long">
        <item name="android:background">@drawable/mine_item_divider</item>
    </style>

    <style name="Mine_Item">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:background">@drawable/ripple_white_gray_bg</item>
    </style>
</resources>
