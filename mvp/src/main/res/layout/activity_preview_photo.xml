<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".module.common.PreviewPhotoActivity">

    <com.qiufg.mvp.wedget.CustomViewPager
        android:id="@+id/viewPager"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <include layout="@layout/layout_toolbar_preview_image" />

    <ImageView
        android:id="@+id/iv_image_preview_download"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/preview_photo_btn_download"
        android:contentDescription="@null"
        android:padding="6dp"
        android:src="@mipmap/icon_download" />
</FrameLayout>